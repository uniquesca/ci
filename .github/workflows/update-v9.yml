name: Update v9 branch

on:
  push:
    branches:
      - main

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config --local user.email "service@uniques.ca"
          git config --local user.name "CI"

      - name: Merge main into v9
        run: |
          git fetch origin v9:v9
          git checkout v9
          git merge origin/main -m "CI: merge main into v9 [skip-ci]"
          git push origin v9
