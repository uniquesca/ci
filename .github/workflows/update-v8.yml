name: Update v8 branch

on:
  push:
    branches:
      - main

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config --local user.email "service@uniques.ca"
          git config --local user.name "CI"

      - name: Merge main into v8
        run: |
          git fetch origin v8:v8
          git checkout v8
          git merge origin/main -m "CI: merge main into v8 [skip-ci]"
          git push origin v8
